@model FreebirdTech.Models.Portfolio

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>Portfolio</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="PortfolioType" class="control-label"></label>
                <input asp-for="PortfolioType" class="form-control" />
                <span asp-validation-for="PortfolioType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EmbedLink" class="control-label"></label>
                <input asp-for="EmbedLink" class="form-control" />
                <span asp-validation-for="EmbedLink" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
